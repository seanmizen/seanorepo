tunnel: 0f63e5e3-ee1c-425a-9916-cd037524731d
credentials-file: ./credentials/0f63e5e3-ee1c-425a-9916-cd037524731d.json

# Cloudflared Ingress Rules (Port Range: 4xxx)
# !! PORT 4040 IS BANNED — ngrok's web inspector binds it by default. Never assign any service to 4040.
# To add a new service:
# 1. Choose an available port in the 4xxx range (e.g., 4070 for frontend, 4071 for backend)
# 2. Add hostname rules following the pattern below
# 3. API requests (/api/* or /ws/*) should route to backend port
# 4. All other requests route to frontend port
# 5. Include wildcard subdomain rules if needed

ingress:
  # planning-poker (pp.seanmizen.com) - Ports 4030 (FE) / 4031 (BE)
  # MUST come before *.seanmizen.com wildcard!
  - hostname: pp.seanmizen.com
    path: /api/*
    service: http://localhost:4031
  - hostname: pp.seanmizen.com
    path: /ws/*
    service: http://localhost:4031
  - hostname: pp.seanmizen.com
    service: http://localhost:4030

  # minecraft.seanmizen.com - Port 4042 (Frontend only, static WebGPU voxel)
  # MUST come before *.seanmizen.com wildcard below — first match wins.
  # Note: 4040 is reserved by ngrok's web inspector — do not use.
  - hostname: minecraft.seanmizen.com
    service: http://localhost:4042

  # inside.seanmizen.com - Ports 4060 (FE) / 4061 (BE)
  # MUST come before *.seanmizen.com wildcard below — first match wins.
  - hostname: inside.seanmizen.com
    path: /api/*
    service: http://localhost:4061
  - hostname: inside.seanmizen.com
    service: http://localhost:4060

  # seanmizen.com - Port 4000 (Frontend only)
  # tcp-getter at tcp/
  - hostname: seanmizen.com
    path: /tcp/*
    service: http://localhost:4120
  - hostname: seanmizen.com
    service: http://localhost:4000
  - hostname: "*.seanmizen.com"
    service: http://localhost:4000

  # carolinemizen.art - Ports 4020 (FE) / 4021 (BE)
  - hostname: carolinemizen.art
    path: /api/*
    service: http://localhost:4021
  - hostname: carolinemizen.art
    service: http://localhost:4020
  - hostname: "*.carolinemizen.art"
    path: /api/*
    service: http://localhost:4021
  - hostname: "*.carolinemizen.art"
    service: http://localhost:4020

  # Catch-all for unmatched routes
  - service: http_status:404
