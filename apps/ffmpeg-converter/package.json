{
  "name": "ffmpeg-converter",
  "private": true,
  "version": "0.0.1",
  "workspaces": [
    "web"
  ],
  "scripts": {
    "start": "concurrently --prefix \"{time} {name} \u2020\" -k --kill-signal SIGKILL -t \"HH:mm\" -n \"FE,BE\" -c \"green.bold,red.bold\" \"yarn workspace ffmpeg-converter-next dev\" \"go run .\"",
    "start:docker": "PUBLISH_ADDR=0.0.0.0 BUILD_TARGET=dev docker compose --profile dev up --build",
    "prod:docker": "docker network create edge >/dev/null 2>&1; BUILD_TARGET=prod docker compose --profile prod up --build --detach",
    "down": "docker compose --profile dev --profile prod down"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
