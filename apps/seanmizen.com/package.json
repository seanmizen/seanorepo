{
  "name": "seanmizen.com",
  "version": "0.2.0",
  "private": true,
  "scripts": {
    "start": "rsbuild dev --open",
    "start:noopen": "rsbuild dev --port 4243",
    "start:docker": "docker compose --profile dev up --build",
    "prod:docker": "BUILD_TARGET=prod docker compose --profile prod up --build",
    "build": "rsbuild build",
    "preview": "rsbuild preview"
  },
  "dependencies": {
    "number-to-words": "^1.2.4",
    "react": "^19.1.0",
    "react-collapsible": "^2.10.0",
    "react-dom": "^19.1.0",
    "react-router-dom": "^7.5.0"
  },
  "devDependencies": {
    "@rsbuild/core": "^1.3.5",
    "@rsbuild/plugin-react": "^1.2.0"
  }
}
