{
  "name": "egstack",
  "private": true,
  "scripts": {
    "ensure": "bash ./ensure-egstack.sh",
    "start": "yarn ensure && yarn --cwd ../../other-repos/egstack start",
    "start:docker": "yarn ensure && yarn --cwd ../../other-repos/egstack start:docker",
    "prod:docker": "yarn ensure && yarn --cwd ../../other-repos/egstack prod:docker",
    "down": "yarn ensure && yarn --cwd ../../other-repos/egstack down"
  }
}
