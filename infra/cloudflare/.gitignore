# The pinned OpenTofu binary and the provider plugins.
.tofu/
.terraform/
# The API token.
.secrets
# The state is a local cache. The import blocks rebuild it (README.md).
*.tfstate
*.tfstate.backup
*.tfstate.*.backup
# Plans hold the same data as the state.
*.tfplan
