nodeLinker: node-modules

# helps with dockerisation:
supportedArchitectures:
  os: [darwin, linux]
  cpu: [arm64, x64]
  libc: [glibc, musl]

# The repository carries its own Yarn, so any `yarn` on the PATH runs this
# release. No corepack.
yarnPath: .yarn/releases/yarn-4.18.0.cjs
